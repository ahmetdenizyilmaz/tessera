# Faster linking on Windows/MSVC: rust-lld roughly halves link time, which
# dominates incremental rebuilds. Delete this file to fall back to link.exe.
[target.x86_64-pc-windows-msvc]
linker = "rust-lld"
